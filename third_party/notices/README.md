# Dependency notices

License texts copied from the exact spike revisions in [`dependencies.lock.json`](../../dependencies.lock.json). Their presence documents provenance; it does not mean either dependency is bundled in `My DAW.app`.

The VST3 SDK notice records a separately bootstrapped, pinned developer
dependency. When its scanner helper is packaged, the full MIT notice is copied
into the application resources.
