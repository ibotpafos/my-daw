# Продукт и опыт работы

## Тезис

Создать музыкальную станцию, в которой запись и редактирование ощущаются непрерывным процессом. Интерфейс должен помогать понимать текущее действие и слышать результат. Расширения добавляют возможности прямо в этот процесс, сохраняя общие правила выбора, отмены, сохранения и доступности.

Первая аудитория — музыкант, который записывает вокал поверх инструментала, собирает дубли, делает обработку и выпускает стереомикс. Это стартовая специализация; архитектура не должна запрещать будущие MIDI-инструменты, но их отсутствие в первой версии принимается явно.

## Принципы

1. Запись надёжнее любой вспомогательной функции. Фоновый анализ уступает ресурсы записи.
2. Прямое действие всегда доступно: мышь и клавиатура работают без ИИ и сети.
3. Выбор определяет контекст: дорожка, клип, диапазон и группа имеют понятные действия.
4. Изменения обратимы. Исходное аудио сохраняется; пакетная операция — один шаг Undo.
5. Сложность раскрывается по запросу. Микшер и маршрутизация доступны без превращения основного экрана в панель всех возможностей.
6. Проект принадлежит музыканту: локальные файлы, экспорт дорожек, документированный формат.
7. Расширяемость не отменяет единый дизайн. Модули используют предоставленные компоненты и команды.

## Что означает «вайб» измеримо

| Намерение | Наблюдаемое поведение | Начальный критерий |
|---|---|---|
| Начать записывать | После первоначального выбора устройства создать дорожку и начать запись | Не более трёх явных действий от открытого пустого проекта |
| Быстро поправить фразу | Split, trim, fade в контексте выделения | Без открытия отдельного редактора |
| Не бояться эксперимента | Создать вариант, сравнить, отменить | Исходник не меняется; Undo восстанавливает проект |
| Не терять внимание | Нет модальных окон на штатном маршруте записи | Ошибки устройства видны рядом с транспортом |
| Получить расширение | Действие появляется у подходящего выделения | Не требуется изучать отдельный интерфейс модуля |

Это гипотезы для юзабилити-проверки, а не доказанные преимущества над конкурентами.

## Основной экран

Постоянно видны транспорт, дорожки и таймлайн. Слева — компактные заголовки дорожек с input/arm/monitor/mute/solo. Снизу по требованию открываются цепочка эффектов или микшер. Справа — контекст выбранного объекта, если ему действительно нужны настройки. Палитра команд вызывается с клавиатуры, но основные операции остаются видимыми.

Тёмная и светлая темы используют системную типографику и семантические цвета. Цвет дорожки — вторичный ориентир, статус записи имеет отдельный текст/символ. Не копировать графику, иконки и точную компоновку Logic. Его аккуратность — ориентир качества; собственную композицию проверять на реальных проектах.

Начальные размеры для прототипа: шаг отступов 4 pt, основные интервалы 8/12/16 pt, высота дорожки 64 pt с диапазоном 40–240 pt, текст основного интерфейса 13 pt. Это стартовые токены, не замороженная дизайн-система. Проверять на 13-дюймовом экране, Retina, внешнем мониторе, при увеличенном тексте. Анимация не должна задерживать редактирование; Reduce Motion соблюдается.

## Навигация и доступность

Два пальца прокручивают таймлайн, pinch масштабирует вокруг позиции указателя, сочетания клавиш дают те же операции. Не перехватывать системные жесты Mission Control. У canvas должна быть accessibility-модель дорожек и клипов, доступная VoiceOver; один красивый Metal-слой этого не обеспечивает. Apple отдельно рекомендует альтернативы жестам для важных действий.[^1]

Транспорт: Space — play/stop, R — record вне текстового ввода, Cmd+Z — Undo, Cmd+Shift+Z — Redo. Точные конфликты shortcuts проверить в AppKit; раскладки RU/EN и редактирование названий включить в проверку.

## Идеи по очереди полезности

| Идея | Польза | Этап / условие |
|---|---|---|
| Take lanes и comp | Собрать лучшие фразы без копий дорожек | Пользовательская альфа |
| Выравнивание дублей | Убрать повторяющийся монтаж | После устойчивого comp, с A/B и прослушиванием |
| Несколько вариантов обработки | Сравнивать решения без потери исходного | После транзакционной модели |
| Подготовка вокала | Найти паузы, пики, проблемные границы | Сначала анализ и preview, без автоматического удаления |
| Подсказки по маршрутизации | Создать общий vocal bus и send | После шин и команды batch |
| Reference A/B | Сравнивать с учётом громкости | После базового микшера |
| Палитра музыкальных намерений | Управлять существующими операциями языком | После command API |
| Контекстный мастеринг | Диагностика клиппинга и варианты финализации | После измерителей и проверенного экспорта |
| Автоматическая расстановка дублей по тексту | Навигация по песне | Отдельный ASR-эксперимент на русском вокале |
| MIDI/piano roll/инструменты | Композиция внутри станции | Следующая продуктовая фаза |
| Совместная работа через сеть | Редактировать вместе | Не входит в начальную архитектурную реализацию |

По прежнему продуктовому направлению ДУБЛЬ полезен принцип «выделение → действие → результат». Переиспользование реализации нельзя планировать как готовую зависимость без проверки кода и качества звука.

## Как исследовать недостатки существующих DAW

Начать с 5 собственных сессий и 6–10 интервью с вокалистами и инженерами. Это разведочное исследование, не репрезентативная статистика. Записывать намерение, последовательность операций, затраченное время, потерянный контекст, способ обхода и важность. Отделять отсутствие знания shortcut от действительного ограничения продукта.

Сравнивать одинаковые задачи: старт записи, выбор фразы из трёх дублей, изменение send, восстановление после сбоя, перенос проекта. Приоритизировать частоту × тяжесть × влияние на завершение песни; сложность реализации учитывать отдельно. Документировать также случаи, когда существующая DAW решает задачу лучше.

[^1]: Apple, [Handling Trackpad Events](https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/EventOverview/HandlingTouchEvents/HandlingTouchEvents.html), архивное описание AppKit; актуальность методов проверяется в SDK.
