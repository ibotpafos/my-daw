# Фоновое сохранение и восстановление — 0.5.0

14 сентября 2026. Save записывает независимый снимок в рабочем потоке. Во время записи можно редактировать проект: завершение отмечает сохранённой только захваченную revision, более новые изменения остаются несохранёнными.

## Поведение

- Для изменённого проекта резервная копия обновляется с интервалом 5 секунд в `~/Library/Application Support/My DAW/Recovery`. При занятом worker новая задача ожидает следующего опроса, очередь снимков не растёт.
- Одновременно допускаются одна ручная запись и одна резервная. Обе используют копию метаданных и shared immutable PCM, без доступа worker к Session или аудиоустройству.
- После аварийного завершения приложение предлагает последнюю копию от уже завершённого процесса. Восстановление открывает её как новый несохранённый проект. Исходный файл не перезаписывается; Undo не восстанавливается.
- Команда «Восстановить черновик…» доступна также в меню. «Позже» оставляет копию на диске.
- При закрытии, New или Open приложение проверяет несохранённые изменения. Во время записи нужно дождаться завершения и повторить действие. Если за время сохранения появились новые правки, перед выходом они проверяются снова.

## Запись

C API предоставляет `daw_begin_save`, `daw_poll_save`, `daw_release_save`. Результат содержит revision, состояние pending/success/failure и текст ошибки. Release освобождает handle без ожидания или отмены потока; снимок принадлежит worker до завершения. Начиная с 1.2 save и export делят [общий bounded budget](27-bounded-background-jobs.md).

SQLite v3 собирается в памяти, сериализуется в уникальный временный файл на том же томе (на macOS — системный item replacement directory), записывается полностью, синхронизируется через `fsync`, закрывается и заменяет целевой через atomic rename. Старый файл остаётся целым до rename. Reader и совместимость v1/v2/v3 сохранены.

Это также устраняет использование file-backed SQLite journal при Save. На этом Mac прежний writer повторно задержался в операции открытия каталога; вынесение в worker позволило продолжать редактирование и обновлять recovery. После замены writer ручное сохранение в прежнем каталоге Documents завершилось. Универсальное устранение задержек всех файловых систем этим не доказано.

## Проверки

Все четыре CTest targets прошли в Debug и ASan/UBSan. Новый `storage_jobs_crash` проверяет SIGKILL после создания временного файла, после полной записи/fsync и после rename: после перезапуска читается соответственно старый, старый и новый валидный файл. Также проверены независимость снимка от последующей правки и уничтожения Session, ошибка записи и roundtrip recovery.

В нативном приложении дважды принудительно завершён только специально созданный тестовый проект. После первого сбоя восстановлена revision 2 с дорожкой Recovery smoke. Во время ожидающей ручной записи добавлена вторая дорожка, recovery обновился до revision 3; после второго сбоя обе дорожки восстановились. Финальный writer сохранил их в `build/Recovery smoke final.mydawdraft`, UI сообщил об успехе, SQLite quick_check вернул ok. Затем в приложении снова открыт пользовательский Audio demo - recovered, revision 16, Audio check, −4 dB. Пользовательский файл в crash-тестах не изменялся.

## Границы

Потеряться могут изменения после последней завершённой резервной копии: интервал 5 секунд плюс задержка записи, без жёсткой верхней границы при ошибке диска. Это проверка падения процесса, не отключения питания: directory fsync/F_FULLFSYNC не реализованы. Нет журнала команд, multi-writer locking, отмены зависшего I/O, выбора из списка всех recovery-копий и автоматического удаления старых цепочек после нескольких падений. Повторное использование PID может консервативно скрыть старую копию.

Открытие/импорт остаются синхронными. Построение SQLite и сериализация требуют дополнительной памяти пропорционально проекту; размер готового файла ограничен 72 MiB, но это не предел суммарного RAM. Одновременно работающие два writer увеличивают пик. Проверки максимального RAM/дисковой нагрузки и физических задержек аудио ещё впереди.

B-004 выполнен для экспериментального snapshot/recovery, полноценный пакет проекта остаётся отдельной задачей. Следующий музыкальный срез: Split, несколько клипов на дорожке и fades.

## Повторные запросы доступа к Documents

Подтверждённый тип запроса: My DAW просит доступ к «Документам». В текущей сборке `codesign -d -r-` показывает designated requirement, привязанный к cdhash, а `security find-identity -v -p codesigning` не нашёл сертификатов. По [Apple TN3127](https://developer.apple.com/documentation/technotes/tn3127-inside-code-signing-requirements), ad-hoc requirement привязан к конкретной версии кода; он не обеспечивает непрерывную идентичность обновлений для privacy permissions.

Для новых проектов Save теперь предлагает `~/Library/Application Support/My DAW/Projects`, recovery уже использует соседний каталог. Выбранный ранее файл сохраняется по своему пути; ничего автоматически не перемещается. Доступ к Documents по-прежнему может потребоваться при явном открытии/сохранении там.

Build script поддерживает `DAW_SIGNING_IDENTITY` для установленного Apple Development/Developer ID сертификата и проверяет результат подписи. Без сертификата остаётся ad-hoc с явным предупреждением. Стабильная подпись и проверка сохранения разрешений между двумя обновлениями пока не выполнены. TCC не сбрасывается, Full Disk Access не запрашивается, designated requirement не ослабляется до одного bundle ID.

Финальная правка macOS writer использует `NSItemReplacementDirectory` через небольшой Objective-C++ адаптер. Временные файлы больше не создаются произвольными соседями проекта в Documents; атомарный rename и fsync сохранены. API выбирает каталог на нужном томе: [Apple FileManager](https://developer.apple.com/documentation/Foundation/FileManager/url%28for%3Ain%3AappropriateFor%3Acreate%3A%29). Каталог удаляется после штатного завершения; при SIGKILL может оставаться системный временный файл.

После этой правки повторно прошли 4/4 Debug и 4/4 ASan/UBSan. Crash-тест запускает отдельный executable через posix_spawn: вызовы Foundation в процессе после fork оказались непригодны, поэтому тест теперь проверяет чистый новый процесс. В UI сохранены тест в Application Support и отдельный файл Permissions Documents smoke в Documents, затем добавлена дорожка и выполнен повторный Save: revision 2, quick_check ok, нового запроса папки в этом сценарии не наблюдалось. TCC для проверки не сбрасывался, поэтому это не доказательство поведения на чистой установке. Обновлённая основная сборка запущена с пользовательской сессией revision 16.
